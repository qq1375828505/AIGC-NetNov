// AI Prompt 模板

export const PROMPT_TEMPLATES = {
  // 续写提示词
  continue_writing: (content: string, context?: string) => `
你是一个专业的网文写作助手。请根据以下前文内容进行续写。

${context ? `作品背景：${context}\n` : ''}
前文内容：
${content}

要求：
1. 保持原文风格和语气
2. 情节连贯，不突兀
3. 适当设置悬念和爽点
4. 字数控制在 500-1000 字

请直接输出续写内容，不要添加额外说明。
`,

  // 润色提示词
  polish_text: (content: string) => `
你是一个专业的文本润色助手。请对以下文本进行润色。

原文：
${content}

要求：
1. 保持原意不变
2. 优化语句表达
3. 增强文学性
4. 修正语法错误

请直接输出润色后的文本。
`,

  // 扩写提示词
  expand_text: (content: string) => `
你是一个专业的文本扩写助手。请对以下文本进行扩写。

原文：
${content}

要求：
1. 保持核心情节不变
2. 增加细节描写
3. 丰富人物对话
4. 扩展场景描写

请直接输出扩写后的文本。
`,

  // 去 AI 味提示词
  deai_flavor: (content: string) => `
你是一个专业的文本改写助手。请对以下文本进行改写，去除 AI 生成的痕迹。

原文：
${content}

要求：
1. 使用更自然的表达
2. 增加口语化表达
3. 避免过于工整的句式
4. 加入个性化表达

请直接输出改写后的文本。
`,

  // 爽点检查提示词
  check_pleasure: (content: string) => `
你是一个网文爽点分析助手。请分析以下文本的爽点分布。

文本：
${content}

请从以下维度分析：
1. 爽点类型（打脸/逆袭/揭秘/升级等）
2. 爽点强度（1-5 分）
3. 爽点密度（每千字爽点数）
4. 改进建议

请以 JSON 格式输出分析结果。
`,

  // 水文检测提示词
  detect_water: (content: string) => `
你是一个网文质量检测助手。请检测以下文本是否存在"水文"问题。

文本：
${content}

请检测以下问题：
1. 重复啰嗦的表达
2. 无意义的填充词
3. 拖沓的情节
4. 缺乏信息量的段落

请以 JSON 格式输出检测结果和修改建议。
`,

  // 标题生成提示词
  generate_title: (content: string, genre: string = "") => `
你是一个爆款标题生成助手。请根据以下内容生成吸引人的标题。

内容摘要：
${content}
${genre ? `\n作品类型：${genre}` : ''}

要求：
1. 有悬念感
2. 符合网文风格
3. 突出核心卖点
4. 10-20 字以内

请生成 5 个备选标题。
`,

  // 大纲生成提示词
  generate_outline: (idea: string, genre: string = "") => `
你是一个网文大纲生成助手。请根据以下创意生成详细大纲。

创意：
${idea}
${genre ? `\n作品类型：${genre}` : ''}

请生成包含以下内容的大纲：
1. 核心设定（世界观、主角、金手指）
2. 主线剧情（起承转合）
3. 主要配角
4. 关键转折点
5. 爽点设计

请以 JSON 格式输出大纲。
`,

  // 角色设计提示词
  design_character: (name: string, role: string, genre: string = "") => `
你是一个角色设计助手。请设计一个详细的角色卡。

角色信息：
- 名字：${name}
- 定位：${role}
${genre ? `- 作品类型：${genre}` : ''}

请设计包含以下内容的角色卡：
1. 基本信息（年龄、性别、外貌）
2. 性格特点
3. 背景故事
4. 能力/技能
5. 人际关系
6. 成长弧线

请以 JSON 格式输出角色卡。
`
};
